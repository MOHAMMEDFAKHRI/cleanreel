# Backend API — light build that runs on a free 512MB tier (classical engine).
fastapi
uvicorn[standard]
python-multipart
opencv-python-headless      # headless = no GUI libs; correct for servers
numpy
imageio-ffmpeg
stripe                      # credit-pack checkout + webhooks
psycopg[binary]             # Neon Postgres (accounts + credit balances)

# Neural LaMa (best quality). Ships in the image; used only when WR_ENGINE=auto on a
# 2 GB+ instance. torch (CPU) is installed separately in the Dockerfile.
simple-lama-inpainting
pillow
